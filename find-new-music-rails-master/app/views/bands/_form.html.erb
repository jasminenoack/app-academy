<% if band.persisted? %>
  <%= form_open(band_url(band)) %>
  <%= hidden_patch %>
<% else %>
  <%= form_open(bands_url) %>
<% end %>

  <%= auth_token %>

  <label for="name">Name</label>
  <input type="text" id="name" name="band[name]" value="<%= band.name %>">
<br>
  <%= button("Update Band") %>

</form>
