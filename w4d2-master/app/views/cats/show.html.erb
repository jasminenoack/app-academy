<h1><%= "Here comes #{@cat.name}"  %></h1>

<table>
  <tr>
     <th> Name:</th>
     <th>Color</th>
     <th>Sex</th>
     <th>Age</th>
  </tr>

  <tr>
    <td><%= @cat.name %></td>

    <td> <%= @cat.color %> </td>

    <td> <%= @cat.sex %> </td>

    <td> <%= @cat.age %> </td>
  </tr>

</table>


<%= render template: "cat_rental_requests/index" %>



<p><%= @cat.description %></p>

<a href="<%= new_cat_rental_request_url %>">Request a cat</a>
<br>
<a href="<%= edit_cat_url(@cat) %>">Edit</a>
<form action="<%=cat_url(@cat)%>" method="post">
  <input type="hidden" name="_method" value="delete">
  <button>DELETE CAT</button>
  <input type="hidden" name="authenticity_token"
          value="<%= form_authenticity_token %>" >
</form>

<a href="<%= cats_url %>" > Back </a>
